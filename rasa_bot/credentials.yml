# Rasa Credentials for Input Channels

# REST channel (για το chatbot widget)
rest:
  # No authentication required for REST channel

# Socket.IO channel (αν χρειαστεί real-time)
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true

# Callback channel (για webhooks)
# callback:
#   # URL που θα στέλνει τα messages
#   # Χρήσιμο αν θέλεις να στείλεις responses σε external service

# Rasa X / Enterprise (αν χρησιμοποιείς)
# rasa:
#   url: "http://rasa-x:5002"
